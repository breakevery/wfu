<Window x:Class="WFU.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:behaviors="clr-namespace:WFU.Host.Behaviors"
        Title="WFU — WebView For Unity" Height="720" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: 菜单栏 -->
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="New" Command="{Binding NewFileCommand}"/>
                <MenuItem Header="Open" Command="{Binding OpenFileCommand}"/>
                <MenuItem Header="Save" Command="{Binding SaveFileCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding ExitAppCommand}"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Undo" Command="{Binding UndoCommand}"/>
                <MenuItem Header="Redo" Command="{Binding RedoCommand}"/>
                <Separator/>
                <MenuItem Header="Cut" Command="{Binding CutCommand}"/>
                <MenuItem Header="Copy" Command="{Binding CopyCommand}"/>
                <MenuItem Header="Paste" Command="{Binding PasteCommand}"/>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Toggle Left Panel" Command="{Binding ToggleLeftPanelCommand}"/>
                <MenuItem Header="Toggle Right Panel" Command="{Binding ToggleRightPanelCommand}"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Row 1: 工具栏 -->
        <ToolBar Grid.Row="1">
            <Button Content="New" Command="{Binding NewFileCommand}"/>
            <Button Content="Open" Command="{Binding OpenFileCommand}"/>
            <Button Content="Save" Command="{Binding SaveFileCommand}"/>
            <Separator/>
            <Button Content="Preview"/>
            <Button Content="Export"/>
        </ToolBar>

        <!-- Row 2: 主内容区（三列） -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Col 0: 左侧文件树（暂留空） -->
            <TreeView x:Name="LeftPanel"
                      Grid.Column="0"
                      BorderThickness="0,0,1,0"
                      BorderBrush="#DDDDDD"/>

            <GridSplitter Grid.Column="1"
                          Width="4"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"/>

            <!-- Col 1: AvalonEdit 编辑器 -->
            <avalonedit:TextEditor x:Name="Editor"
                                   Grid.Column="2"
                                   ShowLineNumbers="True"
                                   FontFamily="Consolas"
                                   FontSize="14"
                                   HorizontalScrollBarVisibility="Auto"
                                   VerticalScrollBarVisibility="Auto"
                                   behaviors:EditorTextBinding.Text="{Binding EditorContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   TextChanged="Editor_TextChanged"/>

            <GridSplitter Grid.Column="3"
                          Width="4"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          ResizeBehavior="PreviousAndNext"/>

            <!-- Col 2: 右侧预览区（暂留空，M3 接入 WebView2） -->
            <Border x:Name="RightPanel"
                    Grid.Column="4"
                    BorderThickness="1,0,0,0"
                    BorderBrush="#DDDDDD"
                    Background="#FAFAFA">
                <TextBlock Text="预览区（待接入 WebView2）"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="Gray"/>
            </Border>
        </Grid>

        <!-- Row 3: 分隔条 -->
        <GridSplitter Grid.Row="3"
                      Height="4"
                      HorizontalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext"/>

        <!-- Row 4: 状态栏 -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="行: "/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding CurrentLine}"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="列: "/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding CurrentColumn}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
